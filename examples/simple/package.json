{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@zion/example-simple",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "vite": "5.2.6",
    "vitest": "1.4.0"
  },
  "dependencies": {
    "@zion/coverage-vitest": "workspace:*"
  }
}
